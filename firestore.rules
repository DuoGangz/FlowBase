rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    // Basic signed-in read/write; tighten later to your roles
    match /{document=**} {
      allow read, write: if isSignedIn();
    }
  }
}


